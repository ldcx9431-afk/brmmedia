# yzylauncher 集成开发经验总结

> 基于 IndexTTS2 语音克隆功能的完整集成过程整理。
> 目的：沉淀可复用的方法论，让后续增加新模态/新功能更高效。
> 编写日期：2026-06-15

---

## 一、项目架构认知（先理解再动手）

### 1.1 整体架构

```
用户/前端
   │
   ▼
webui.py (Gradio, 端口9000)
   │  ├─ build_ui()          各模态一个 gr.Tab
   │  ├─ submit_workflow_N() 提交任务到队列
   │  ├─ TaskQueue           单 worker 串行执行
   │  └─ render_queue()      轮询任务状态
   │
   ▼
comfyui_server.py (通用层)
   │  ├─ start_comfyui()     启动 ComfyUI 子进程
   │  ├─ run_workflow()      提交工作流并等待结果
   │  ├─ upload_image()      上传素材到 ComfyUI input 目录
   │  └─ get_view_file()     取回产物文件
   │
   ▼
ComfyUI (端口8188, 独立子进程)
   └─ 执行工作流 JSON, 产出文件到 output 目录
```

### 1.2 关键设计原则

| 原则 | 说明 | 启示 |
|------|------|------|
| **插件式工作流** | 每个模态 = 一个 `build_workflow_N` + 登记 + 一个 Tab | 新增功能只需改 webui.py 后半部分 |
| **任务队列串行** | 单 worker，FIFO，同一时刻只跑一个任务 | 新功能自动获得排队/中断/轮询能力 |
| **产物统一落盘** | 所有产物存到 `outputs/`，文件名以任务名为前缀 | 新功能的产物自动被画廊/下载复用 |
| **前端 API 自动暴露** | Gradio 的 `.click()` 绑定的函数自动成为 API 端点 | 新增 Tab 即获得 `/submit_workflow_N` 端点 |
| **两套 Python 环境** | webui 用 `build_venv`，ComfyUI 用 `python_embeded` | **依赖只装在 python_embeded！** |

### 1.3 两个 Python 环境的区分（重要！）

```
win-unpacked/python/
├── build_venv/          ← webui.py（Gradio）用的环境
│   └── python/python.exe
└── ComfyUI_windows_portable/
    └── python_embeded/  ← ComfyUI 用的环境（装节点依赖的地方！）
        └── python.exe
```

**常见错误**：把 ComfyUI 节点的依赖装到了 build_venv。节点会报 `ModuleNotFoundError`。
**正确做法**：节点依赖一律装到 `python_embeded/python.exe -m pip install`。

---

## 二、新增一个功能的标准 4 步（照抄即可）

以 IndexTTS2 为例，这是验证过的完整流程：

### 第 1 步：准备 ComfyUI 工作流 JSON

1. 在 ComfyUI 界面（8188）搭好工作流，或在节点自带示例基础上修改
2. **必须导出为 API 格式**（不是 UI 格式）：设置里开 Enable API format
3. 保存到 `win-unpacked/python/workflows/<工作流名>.json`

**API 格式长这样**（关键！节点ID → {class_type, inputs}）：
```json
{
  "1": { "class_type": "LoadAudio", "inputs": {"audio": "ref.wav"} },
  "2": { "class_type": "IndexTTS2BaseNode", "inputs": {"text": "...", "reference_audio": ["1", 0]} },
  "3": { "class_type": "SaveAudioMP3", "inputs": {"audio": ["2", 0], "filename_prefix": "TTS"} }
}
```

> 节点连接用 `["源节点ID", 输出索引]` 表示，如 `["1", 0]` = 节点1的第0个输出。

### 第 2 步：在 webui.py 加 build_workflow_N

在 `build_workflow_6` 后面照抄模式，只改节点 ID 映射：

```python
def build_workflow_7(workflow_name: str, args: dict) -> dict:
    import json
    path = WORKFLOW_DIR / (workflow_name + ".json")
    if not path.exists():
        raise gr.Error(f"找不到工作流文件:{path}")
    wf = json.loads(path.read_text(encoding="utf-8"))
    wf["1"]["inputs"]["audio"] = args["ref_audio"]   # 改成你的参数
    wf["2"]["inputs"]["text"] = args["prompt"]
    wf["2"]["inputs"]["seed"] = random.randint(1, 1000000)
    return wf
```

### 第 3 步：加 submit 函数 + 登记到 WORKFLOW_BUILDERS

```python
def submit_workflow_7(prompt, ref_audio, temperature=0.8):
    if not prompt or not prompt.strip():
        raise gr.Error("请输入要合成的文本")
    if not ref_audio:
        raise gr.Error("请上传参考音频")
    submit("TTS-语音克隆", {          # 工作流名(对应JSON文件名，无后缀)
        "prompt": prompt,
        "ref_audio": ref_audio,
        "temperature": temperature,
    })

WORKFLOW_BUILDERS = {
    ...
    "TTS-语音克隆": (build_workflow_7, "语音克隆"),   # ← 登记一行
}
```

### 第 4 步：在 build_ui() 加一个 gr.Tab

照抄现有 Tab，改组件和绑定：
```python
with gr.Tab("语音克隆IndexTTS2"):
    prompt7 = gr.Textbox(label="合成文本", lines=8)
    reference_audio7 = gr.Audio(label="参考音频", type="filepath")
    uploaded_name7 = gr.State("")
    submit_btn7 = gr.Button("提交", variant="primary")
    reference_audio7.upload(fn=on_audio_upload, inputs=reference_audio7, outputs=[uploaded_name7, ...])
    submit_btn7.click(fn=submit_workflow_7, inputs=[prompt7, uploaded_name7])
```

**改完语法检查**：`python -c "import ast; ast.parse(open('webui.py',encoding='utf-8').read())"`

---

## 三、踩过的坑与解决方案（避雷指南）

### 坑 1：transformers 版本不兼容

**现象**：TTS2 节点加载后，生成时报错或无输出。
**根因**：环境是 transformers 5.9，但 IndexTTS2 要求 ≤4.54（Issue #117，官方已知）。
**排查**：
```bash
python_embeded\python.exe -c "import transformers; print(transformers.__version__)"
```
**解决**：降级到 4.54.1。
**关键验证（降级前必做）**：确认现有功能不依赖 transformers。
```powershell
# 搜 ComfyUI 核心 + 所有 custom_nodes 是否 import transformers
Get-ChildItem comfy,custom_nodes -Recurse -Filter *.py | Select-String "import transformers"
```
本项目结果：**零引用**，所以降级安全。

### 坑 2：误判依赖是否必需

**现象**：requirements.txt 里列了 `wetext`、`audiotools`，我搜节点自己的 .py 文件没找到，判断"不需要"，结果 `audiotools` 实际必需，生成时报错。

**根因**：节点 vendor 了第三方库源码（`indextts2/vendor/indextts/...`），里面直接 import 了 audiotools。

**教训**：**判断依赖必须扫 vendor 子目录**，不只看节点顶层文件。
```powershell
# 正确的依赖检查范围
Get-ChildItem custom_nodes\节点目录 -Recurse -Filter *.py | Select-String "import audiotools"
```

### 坑 3：audiotools 需要 git 安装

**现象**：requirements.txt 写 `git+https://github.com/descriptinc/audiotools`，但环境没 git。
**解决**：PyPI 上有镜像包 `descript-audiotools`，直接 pip 装：
```bash
python_embeded\python.exe -m pip install descript-audiotools
```

### 坑 4：模型下载源 hf-mirror 被代理拦截

**现象**：一键下载脚本选了 hf-mirror 镜像，但全部 6/6 下载失败（502/cannot locate）。
**根因**：系统配了代理 `127.0.0.1:7897`（Clash），代理对 hf-mirror 的 https 处理异常。
**排查**：
```powershell
Invoke-WebRequest 'https://hf-mirror.com/...' -Proxy 'http://127.0.0.1:7897'  # 308 重定向，坏
Invoke-WebRequest 'https://www.modelscope.cn/...'                            # OK，直连好
```
**解决（混合源策略）**：
- 5 个模型走 **modelscope 直连**（设 `NO_PROXY=modelscope.cn`）
- BigVGAN 走 **huggingface 官方**（代理 7897 本来就能访问官方 HF）

> modelscope 是阿里魔搭，国内直连最快。验证仓库存在：`modelscope.hub.api.HubApi().get_model_files(repo)`

### 坑 5：下载进程被工具会话回收

**现象**：后台下载任务，进程跑到一半突然消失（python 进程数变 0），日志中断。
**根因**：父进程是 Bash 工具启动的，工具超时/会话结束时连带杀子进程。
**解决**：用 **VBS（VBScript）脱离启动**，进程独立存活：
```vbs
' _launch_detached.vbs
Set sh = CreateObject("WScript.Shell")
sh.CurrentDirectory = "H:\yzylauncher-win-ltx23"
sh.Run "cmd /c start_browser.bat", 0, False   ' 0=隐藏窗口, False=不等待
```
**启动命令**（关键：`start /b` 避免阻塞）：
```cmd
cmd /c "start /b cscript //B //nologo _launch_detached.vbs"
```

### 坑 6：no git 环境

**现象**：`git clone` 失败，环境没装 git。
**解决**：改用 zip 下载解压：
```powershell
Invoke-WebRequest 'https://github.com/xxx/archive/main.zip' -OutFile node.zip
Expand-Archive node.zip -DestinationPath tmp
Move-Item tmp\repo-main custom_nodes\节点目录
```

### 坑 7：bat 文件编码导致 cmd 解析错误

**现象**：自己写的 .bat 含中文注释，运行时一堆 `'ttps:' 不是内部命令` 错误。
**根因**：bat 保存为 UTF-8，cmd 用 GBK 解析，中文字节破坏了命令行。
**解决**：bat 文件要么用 **GBK(GB18030) 编码**保存，要么**纯 ASCII**（推荐，注释用英文）。

### 坑 8：Gradio 的 gr.State 不暴露为 API 参数

**现象**：`/submit_workflow_7` 端点暴露了，但参数少了 `ref_audio`（参考音频）。
**根因**：`ref_audio` 用了 `gr.State("")` 存上传后的文件名，State 是内部状态，不作为 API 参数。
**结论**：这是**整个项目的统一设计**（Tab 2/4/5/6 都这样），API 调用方需先调上传端点拿文件名，再调提交端点。**符合现有模式，不需要"修复"**。

---

## 四、关键命令速查

### 4.1 环境检查

```powershell
# 检查 python_embeded 的关键库版本
python_embeded\python.exe -c "import transformers,torch; print(transformers.__version__, torch.__version__)"

# 检查 ComfyUI 已注册的节点
Invoke-WebRequest 'http://127.0.0.1:8188/object_info' | ConvertFrom-Json

# 检查某节点参数
Invoke-WebRequest 'http://127.0.0.1:8188/object_info/节点名'

# 检查 Gradio 暴露的 API
Invoke-WebRequest 'http://127.0.0.1:9000/gradio_api/info'
```

### 4.2 依赖安装（务必用 python_embeded）

```bash
set EMB=...\python_embeded\python.exe
# 用清华镜像加速
%EMB% -m pip install 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 4.3 模型下载

```python
# modelscope（推荐，国内直连）
from modelscope.hub.snapshot_download import snapshot_download
snapshot_download(model_id="仓库名", local_dir="目标目录", revision='master')

# huggingface（走系统代理）
os.environ['HF_ENDPOINT'] = 'https://huggingface.co'
from huggingface_hub import snapshot_download
snapshot_download(repo_id="仓库名", local_dir="目标目录")
```

### 4.4 服务管理

```powershell
# 停服务
Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
  Where-Object { $_.CommandLine -match 'entry_yzy|main.py' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }

# 启服务（VBS 脱离方式，保活）
cmd /c "start /b cscript //B //nologo _launch_detached.vbs"

# 验证
Get-NetTCPConnection -State Listen -LocalPort 8188,9000
```

### 4.5 工作流测试（直接调 ComfyUI，绕过 Gradio）

```python
import json, requests, time
wf = json.load(open('工作流.json', encoding='utf-8'))
r = requests.post('http://127.0.0.1:8188/prompt',
                  json={'prompt': wf, 'client_id': 'test'}).json()
pid = r['prompt_id']  # node_errors 为空 = 工作流正确
# 轮询结果
h = requests.get(f'http://127.0.0.1:8188/history/{pid}').json().get(pid)
```

---

## 五、文件清单（这次新增/修改了什么）

### 新增文件
| 文件 | 用途 |
|------|------|
| `win-unpacked/python/workflows/TTS-语音克隆.json` | IndexTTS2 API 格式工作流 |
| `ComfyUI/.../custom_nodes/ComfyUI-Index-TTS/` | IndexTTS2 节点（含 vendor 的推理库） |
| `ComfyUI/.../models/IndexTTS-2/` | 模型权重（10.68 GB） |
| `_launch_detached.vbs` | VBS 启动脚本（保活用，勿删） |
| `经验.md` | 本文档 |

### 修改文件
| 文件 | 改动 |
|------|------|
| `webui.py` | +`build_workflow_7` +`submit_workflow_7` +`WORKFLOW_BUILDERS`登记 +Tab 7 |
| `启动.bat` / `debug.bat` | 补 `NO_PROXY` 代理绕过 + 修路径变量 bug |
| `start_browser.bat` | 新增：绕过 Electron 的直启动脚本 |

### 环境变更（python_embeded 内）
| 包 | 变更 |
|----|------|
| transformers | 5.9 → **4.54.1**（降级，TTS2 必需） |
| 新增 | librosa, soundfile, modelscope, json5, munch, ffmpy, docstring-parser, descript-audiotools |

---

## 六、后续可扩展功能建议

按集成难度从低到高排序：

| 功能 | 难度 | 说明 |
|------|------|------|
| IndexTTS2 情感控制 | 低 | 已有节点，加几个参数到 Tab 即可 |
| TTS2 多角色朗读（小说） | 低 | 节点 `IndexTTS2ProNode` 已就绪 |
| ASR 语音转文字 | 中 | 加 ComfyUI 的 Whisper 节点 |
| 音频降噪/分离增强 | 低 | MelBandRoFormer 已装，加工作流即可 |
| 更多图片/视频模型 | 中 | 换 checkpoint/工作流 JSON |
| TTS + 数字人联动 | 中 | 串联 workflow_7 → workflow_6 |
| 批量处理/队列API | 中 | 扩展 TaskQueue 支持批量入队 |

### 扩展时的检查清单

每次加新功能，对照检查：
- [ ] ComfyUI 节点已安装并重启验证（日志无 IMPORT FAILED）
- [ ] 依赖装到了 **python_embeded**（不是 build_venv）
- [ ] 扫了 vendor 子目录确认无遗漏依赖
- [ ] 工作流 JSON 是 **API 格式**（不是 UI 格式）
- [ ] webui.py 语法检查通过
- [ ] 重启后 `/gradio_api/info` 出现新端点
- [ ] 实际提交一次，确认产出文件生成

---

## 七、参考资料

| 项目 | 链接 |
|------|------|
| IndexTTS2 ComfyUI 节点 | https://github.com/chenpipi0807/ComfyUI-Index-TTS |
| IndexTTS2 模型 | https://huggingface.co/IndexTeam/IndexTTS-2 |
| modelscope（模型镜像） | https://www.modelscope.cn |
| ComfyUI API 文档 | https://docs.comfy.org/ |
| Gradio API 调用方式 | https://www.gradio.app/guides/querying-gradio-apps-with-via-api |

---

*本文档随项目演进持续更新。新增功能时，补充对应的坑和经验到第三节。*
